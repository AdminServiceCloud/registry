{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://registry.adminservice.cloud/schema/asc.settings.schema.json",
  "title": "ASC Application Settings (asc.settings.yaml)",
  "description": "Application settings definition referenced from asc.yaml via 'settings: ./asc.settings.yaml'. Users fill these values at install time (platform UI renders a form, CLI asks interactively, silent mode uses defaults).",
  "type": "object",
  "anyOf": [{ "required": ["settings"] }, { "required": ["quota"] }],
  "properties": {
    "quota": {
      "type": "object",
      "description": "Resource quota applied to the app instance at install/upgrade time (max CPU, RAM, disk)",
      "properties": {
        "max_cpu": { "type": "number", "exclusiveMinimum": 0, "description": "CPU cores limit, e.g. 0.5 or 2 (Docker: NanoCpus)" },
        "max_ram": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?\\s*([KMGTkmgt][Bb]?|[KMGTkmgt][Ii][Bb]|[Bb])?$", "description": "Memory limit as a size string, e.g. 512M or 2G (Docker: Memory)" },
        "max_disk": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+)?\\s*([KMGTkmgt][Bb]?|[KMGTkmgt][Ii][Bb]|[Bb])?$", "description": "Disk usage limit, e.g. 10G (recorded; per-runtime enforcement is incremental)" }
      },
      "additionalProperties": false
    },
    "start_command": {
      "type": "string",
      "description": "Start command override; supports ${VAR} interpolation from the app environment (applied with DMN-018)"
    },
    "settings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["key", "type"],
        "properties": {
          "key": { "type": "string", "pattern": "^[a-z0-9][a-z0-9_]*$", "description": "Setting identifier" },
          "type": {
            "type": "string",
            "enum": ["string", "number", "boolean", "enum", "secret", "ports", "volumes"],
            "description": "Setting type; 'secret' values are stored encrypted and masked in UI/logs; 'ports' is a list of published container ports (with env: the list is exposed comma-joined; see 'protocol' to choose tcp/udp/both); 'volumes' is a list of /container/path[:host_folder|:/host/path] or name:/container/path[:ro] entries"
          },
          "title": { "type": "string", "description": "Human-readable label (English for public registries)" },
          "description": { "type": "string", "description": "Help text shown next to the field" },
          "default": { "description": "Default value; required for silent installs unless 'required' is true. For 'ports' and 'volumes' — a list" },
          "required": { "type": "boolean", "default": false },
          "values": {
            "type": "array",
            "items": {},
            "description": "Allowed values (type: enum)"
          },
          "allow_custom": {
            "type": "boolean",
            "default": false,
            "description": "Accept a value outside 'values' as free text (type: enum only), e.g. a custom game branch/world name alongside the common presets"
          },
          "limits": {
            "type": "object",
            "description": "Value constraints",
            "properties": {
              "min": { "type": "number" },
              "max": { "type": "number" },
              "min_length": { "type": "integer" },
              "max_length": { "type": "integer" },
              "pattern": { "type": "string", "description": "Regex the value must match (type: string)" }
            },
            "additionalProperties": false
          },
          "env": { "type": "string", "description": "Environment variable to expose the value as (optional)" },
          "protocol": {
            "type": "string",
            "enum": ["tcp", "udp", "both"],
            "description": "Transport(s) to publish on (type: ports only); defaults to 'tcp'. 'both' forwards the same host==container port on TCP and UDP"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
